const { functionName } = require("../code.js");
//
//
const { assert } = require("chai");
describe("The ___ function should pass...", () => {
  it("...the sample tests", () => {
    // Sample test cases
  });
  it("...more fixed tests", () => {
    // Fixed test cases
  });
  it("...random tests", () => {
    // Random tests
  });
});
